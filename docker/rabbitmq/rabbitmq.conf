# RabbitMQ configuration file for Hathor platform
# Auto-loads declarative topology definitions on daemon startup

management.load_definitions = /etc/rabbitmq/definitions.json

# Default virtual host
default_vhost = /

# Disable non-encrypted loopback restrictions for development
loopback_users.guest = false
